package model.entities.Tecnico;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class RadarDeTecnicos {
  /*
  private List<Tecnico> tecnicos;

  public RadarDeTecnicos(List<Tecnico> tecnicos) {
    this.tecnicos = tecnicos;
  }

  public List<Tecnico> obtenerTecnicosCercanos(Heladera heladera){
    List<Tecnico> tecnicosDisponibles = new ArrayList<>();
    for (Tecnico tecnico : tecnicos) {
      if (tecnico.puedeAcudir(heladera.getPuntoGeografico().getDireccion().getLocalidad())) {
        tecnicosDisponibles.add(tecnico);
      }
    }
    return tecnicosDisponibles;
  }

  public void agregarTecnico(Tecnico tecnico){
    tecnicos.add(tecnico);
  }*/

}
